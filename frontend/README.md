# AI PDF Tutor Frontend

React + Vite frontend that talks to the Python FastAPI backend.

Set `VITE_BACKEND_URL` in a `.env` file for deployment (e.g. to Vercel).

Locally:

```bash
npm install
npm run dev
```
